# Test Failures due possible code bugs

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Benches.UnitTests.TrimmingBenchTests.Setup_WithLengthZero_CompletesSuccessfully
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Benches\TrimmingBenchTests.cs
- **Bug Location**: Performance.Benchmarks\Benches\TrimmingBench.cs@73

### Analysis
The test correctly expects that Setup() should complete successfully when Length=0. However, the BuildBuffer() method has a bug: when Length=0, it creates an empty array (byte[0]) but then sets payload=1 and attempts to write to data[0], causing an IndexOutOfRangeException at line 73. The logic at lines 53-62 forces payload to be at least 1 without checking if the original Length is 0, making it impossible to write the payload to the zero-length array.

### Suggested Fix
The BuildBuffer() method needs to handle the Length=0 edge case. Add a check at the beginning of the method:

```csharp
private byte[] BuildBuffer()
{
    // Handle edge case where Length is 0
    if (Length == 0)
    {
        return new byte[0];
    }
    
    // ... rest of the existing code
}
```

This ensures that when Length is 0, an empty array is returned immediately without attempting to populate it with data.

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableByteWriterTests.GetMemory_LargeNegativeSizeHint_ThrowsArgumentOutOfRangeException
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableByteWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableByteWriter.cs@87

### Analysis
The test correctly expects that GetMemory with a negative sizeHint should throw ArgumentOutOfRangeException, but the production code is throwing OverflowException instead. The issue is in the GetMemory method at line 87-92 which does not validate that sizeHint is non-negative before passing it to Grow(). When sizeHint is int.MinValue, the GrowIfRequired method at line 138 performs checked arithmetic (_index + size) which causes OverflowException. The production code should validate input parameters and throw ArgumentOutOfRangeException for negative values, consistent with the pattern already established in the same class: the constructor validates initialCapacity with ArgumentOutOfRangeException.ThrowIfNegative at line 23, and the Advance method validates count at lines 80-81. The fix is to add parameter validation in GetMemory (and GetSpan) before calling Grow.

### Suggested Fix
The GetMemory method (and similarly GetSpan) needs to validate that sizeHint is not negative before calling Grow. Add validation at the beginning of the GetMemory method:

```csharp
public Memory<byte> GetMemory(int sizeHint = 0)
{
    ArgumentOutOfRangeException.ThrowIfNegative(sizeHint);
    if (sizeHint == 0) sizeHint = 8;
    Grow(sizeHint);
    return new Memory<byte>(_array, _index, sizeHint);
}
```

The same fix should be applied to GetSpan method at lines 94-99:

```csharp
public Span<byte> GetSpan(int sizeHint = 0)
{
    ArgumentOutOfRangeException.ThrowIfNegative(sizeHint);
    if (sizeHint == 0) sizeHint = 8;
    Grow(sizeHint);
    return new Span<byte>(_array, _index, sizeHint);
}
```

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableByteWriterTests.GetMemory_WhenDisposed_ThrowsObjectDisposedException
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableByteWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableByteWriter.cs@15

### Analysis
The test correctly expects that calling GetMemory() on a disposed OriginalResizableByteWriter should throw an ObjectDisposedException. However, the production code has a bug: the _disposed field is marked as 'readonly' on line 15, which means it can only be set in the constructor or during field initialization. The Dispose() method (lines 144-153) checks if (_disposed) but never sets _disposed = true because it cannot modify a readonly field. As a result, when GetMemory() is called after disposal, it calls Grow(), which calls ThrowIfDisposed(), but ThrowIfDisposed() always sees _disposed as false and never throws the exception. The fix is to remove the 'readonly' modifier from the _disposed field declaration on line 15, and add '_disposed = true;' in the Dispose() method after the check 'if (_disposed) return;'.

### Suggested Fix
In the file C:\Users\navy\source\repos\elslush\Performance\Performance.Benchmarks\Original\OriginalResizableByteWriter.cs:

1. On line 15, change `private readonly bool _disposed;` to `private bool _disposed;` (remove the readonly modifier)

2. In the Dispose() method (lines 144-153), add `_disposed = true;` after the check. The corrected Dispose() method should be:

```csharp
public new void Dispose()
{
    if (_disposed) return;
    _disposed = true;
    if (_array != null)
    {
        _pool.Return(_array, RuntimeHelpers.IsReferenceOrContainsReferences<byte>());
        _array = null;
    }
    GC.SuppressFinalize(this);
}
```

This will ensure that the _disposed flag is properly set when Dispose() is called, allowing ThrowIfDisposed() to correctly throw ObjectDisposedException when methods are called on a disposed instance.

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableByteWriterTests.GetSpan_AfterDispose_ThrowsObjectDisposedException
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableByteWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableByteWriter.cs@15

### Analysis
The test is correct in expecting ObjectDisposedException when GetSpan() is called on a disposed object. However, the production code has a bug: the _disposed field is declared as readonly (line 15), which means it can never be changed after initialization. The Dispose() method (line 144-153) checks if (_disposed) but never sets it to true. This means ThrowIfDisposed() will never throw an exception because _disposed is always false. The fix requires: 1) Remove the 'readonly' modifier from the _disposed field (line 15), and 2) Add '_disposed = true;' at the beginning of the Dispose() method after the initial check.

### Suggested Fix
In OriginalResizableByteWriter.cs:
1. Line 15: Change `private readonly bool _disposed;` to `private bool _disposed;`
2. In the Dispose() method (after line 146), add `_disposed = true;` after the `if (_disposed) return;` check.

The corrected Dispose() method should be:
```csharp
public new void Dispose()
{
    if (_disposed) return;
    _disposed = true;
    if (_array != null)
    {
        _pool.Return(_array, RuntimeHelpers.IsReferenceOrContainsReferences<byte>());
        _array = null;
    }
    GC.SuppressFinalize(this);
}
```

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableByteWriterTests.Write_DisposedWriter_ThrowsObjectDisposedException
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableByteWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableByteWriter.cs@15

### Analysis
The test correctly expects that calling Write() on a disposed OriginalResizableByteWriter should throw an ObjectDisposedException. However, the production code has a critical bug: the _disposed field is declared as 'readonly' (line 15), which means it can only be set in the constructor and cannot be modified afterwards. The Dispose() method checks if (_disposed) but never sets it to true because it cannot - the field is readonly. This means the object never actually transitions to a disposed state, and ThrowIfDisposed() never throws. The fix is to remove the 'readonly' modifier from the _disposed field declaration and add '_disposed = true;' in the Dispose() method.

### Suggested Fix
In the file C:\Users\navy\source\repos\elslush\Performance\Performance.Benchmarks\Original\OriginalResizableByteWriter.cs:

1. On line 15, change:
```csharp
private readonly bool _disposed;
```
to:
```csharp
private bool _disposed;
```

2. In the Dispose() method, after line 146, add a line to set the disposed flag. The Dispose() method should be:
```csharp
public new void Dispose()
{
    if (_disposed) return;
    _disposed = true;
    if (_array != null)
    {
        _pool.Return(_array, RuntimeHelpers.IsReferenceOrContainsReferences<byte>());
        _array = null;
    }
    GC.SuppressFinalize(this);
}
```

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableByteWriterTests.Write_NullBuffer_ThrowsArgumentNullException
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableByteWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableByteWriter.cs@30

### Analysis
The test expects ArgumentNullException when a null buffer is passed to Write(byte[], int, int), which is the correct behavior according to .NET conventions for Stream.Write methods. However, the production code at line 30 calls `buffer.AsSpan(offset, count)` without null-checking the buffer first. When buffer is null with offset=0 and count=0, AsSpan returns an empty span without throwing, so no exception occurs. The production code should validate the buffer parameter and throw ArgumentNullException when it's null, as is standard practice for Stream implementations in .NET.

### Suggested Fix
In the OriginalResizableByteWriter.Write(byte[], int, int) method at line 30, add parameter validation before calling AsSpan. The method should be changed to:

```csharp
public override void Write(byte[] buffer, int offset, int count)
{
    ArgumentNullException.ThrowIfNull(buffer);
    Write(buffer.AsSpan(offset, count));
}
```

Alternatively, to match traditional .NET parameter validation:

```csharp
public override void Write(byte[] buffer, int offset, int count)
{
    if (buffer == null)
        throw new ArgumentNullException(nameof(buffer));
    Write(buffer.AsSpan(offset, count));
}
```

This ensures that ArgumentNullException is thrown when buffer is null, matching the expected behavior for Stream.Write implementations and satisfying the test expectations.

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableByteWriterTests.WrittenMemory_AfterDispose_BehavesAsExpected
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableByteWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableByteWriter.cs@15

### Analysis
The production code has a bug where the `_disposed` field is declared as `readonly` (line 15), which means it can only be set in the constructor and cannot be modified later. The `Dispose()` method (line 146) checks `if (_disposed)` but can never set `_disposed = true`. After disposal, `_array` is set to null (line 150), but when `WrittenMemory` is accessed, `ThrowIfDisposed()` doesn't throw because `_disposed` is still false. Then it attempts to create `new ReadOnlyMemory<byte>(_array, 0, _index)` with a null array and non-zero index (3 in this case), which throws `ArgumentOutOfRangeException`. The fix is to remove the `readonly` modifier from `_disposed` field and set `_disposed = true` in the Dispose method before or after returning the array to the pool.

### Suggested Fix
In the file `OriginalResizableByteWriter.cs`:

1. **Line 15:** Change `private readonly bool _disposed;` to `private bool _disposed;` (remove the `readonly` modifier)

2. **Line 144-153:** Modify the `Dispose()` method to set `_disposed = true`:

```csharp
public new void Dispose()
{
    if (_disposed) return;
    _disposed = true;
    if (_array != null)
    {
        _pool.Return(_array, RuntimeHelpers.IsReferenceOrContainsReferences<byte>());
        _array = null;
    }
    GC.SuppressFinalize(this);
}
```

This will ensure that after disposal, `ThrowIfDisposed()` will correctly throw `ObjectDisposedException` when trying to access disposed resources, which is the proper behavior for disposed objects.

## Performance.Benchmarks.UnitTests.csproj - Performance.Benchmarks.Original.UnitTests.OriginalResizableSpanWriterTests.Write_WhenDisposed_ThrowsObjectDisposedException
- **Confidence**: High
- **Test File**: Performance.Benchmarks.UnitTests\Original\OriginalResizableSpanWriterTests.cs
- **Bug Location**: Performance.Benchmarks\Original\OriginalResizableSpanWriter.cs@13,147

### Analysis
The test correctly expects an ObjectDisposedException when Write() is called after Dispose(), but instead a NullReferenceException is thrown. The root cause is in the production code: 1) The _disposed field is declared as readonly (line 13), preventing it from being modified after construction. 2) The Dispose() method (lines 139-148) never sets _disposed = true, so ThrowIfDisposed() never throws. 3) After Dispose() sets _array = null (line 146), subsequent Write() calls trigger Copy() -> Grow() -> ThrowIfDisposed(), but since _disposed is still false, execution continues and attempts to access the null _array, causing a NullReferenceException at line 134 in GrowIfRequired() when it tries to access _array!.Length.

### Suggested Fix
In OriginalResizableSpanWriter.cs, fix the following:

1. At line 13, remove the 'readonly' modifier from the _disposed field:
```csharp
private bool _disposed;
```

2. In the Dispose() method, add a line to set _disposed = true after the disposal logic (after line 147):
```csharp
public void Dispose()
{
    if (_disposed) return;

    if (_array != null)
    {
        _pool.Return(_array, RuntimeHelpers.IsReferenceOrContainsReferences<T>());
        _array = null;
    }
    _disposed = true;
}
```

This ensures that after disposal, the _disposed flag is set to true, and subsequent calls to Write() will properly throw ObjectDisposedException via the ThrowIfDisposed() check in Grow().

## Performance.Tests.csproj - Performance.Tests.ResizableByteWriterTests.Advance_ZeroCountWithZeroReservation_ThrowsInvalidOperationException
- **Confidence**: High
- **Test File**: Performance.Tests\ResizableByteWriterTests.cs
- **Bug Location**: Performance\Buffers\ResizableByteWriter.cs@166

### Analysis
The test correctly expects that calling Advance(0) without a prior reservation (GetSpan/GetMemory call) should throw an InvalidOperationException. The _available field tracks the number of bytes reserved by the last GetSpan/GetMemory call and is initialized to 0. The production code's Advance method only checks 'if (count > _available)' which evaluates to '0 > 0' (false) when count=0 and _available=0, allowing the call to proceed. According to the IBufferWriter<T> pattern and the test's documented intent ('even zero advance requires a valid reservation'), any call to Advance should require a prior reservation. The production code needs to first verify that _available > 0 before allowing any advance operation.

### Suggested Fix
The Advance method in ResizableByteWriter.cs needs to be fixed to validate that a reservation exists before allowing any advance. The current check at line 166 only validates that count doesn't exceed the reservation, but doesn't validate that a reservation exists at all. The fix is to add a check for _available == 0 before the existing check:

```csharp
public void Advance(int count)
{
    ArgumentOutOfRangeException.ThrowIfNegative(count);
    ThrowIfDisposed();

    if (_available == 0)
        throw new InvalidOperationException("Cannot advance without a buffer reservation. Call GetSpan or GetMemory first.");
    
    if (count > _available)
        throw new InvalidOperationException("Cannot advance past the end of the reserved buffer segment.");

    _index += count;
    _available = 0; // The reservation is consumed after advancing.
}
```

Alternatively, the condition could be simplified to:
```csharp
if (_available == 0 || count > _available)
    throw new InvalidOperationException("Cannot advance past the end of the reserved buffer segment.");
```

## Performance.Tests.csproj - Performance.Tests.ResizableByteWriterTests.Length_AfterDispose_ThrowsObjectDisposedException
- **Confidence**: High
- **Test File**: Performance.Tests\ResizableByteWriterTests.cs
- **Bug Location**: Performance\Buffers\ResizableByteWriter.cs@77

### Analysis
The test correctly expects that accessing the Length property after disposal should throw an ObjectDisposedException. This is a standard pattern in .NET for IDisposable types. The ResizableByteWriter class has a ThrowIfDisposed() method (line 276-279) that is called by most methods and properties (Reset, WrittenMemory, WrittenSpan, Advance, GetMemory, GetSpan, Grow), but the Length property (line 77) does not call it. The Length property simply returns _index without checking if the object is disposed. This is inconsistent with the rest of the class design and violates the expected behavior for disposed objects.

### Suggested Fix
The Length property in ResizableByteWriter.cs at line 77 needs to be modified to check for disposal before returning the value. Change:

```csharp
public override long Length => _index;
```

To:

```csharp
public override long Length
{
    get
    {
        ThrowIfDisposed();
        return _index;
    }
}
```

This will ensure that accessing the Length property after disposal throws an ObjectDisposedException, consistent with other properties and methods in the class.

